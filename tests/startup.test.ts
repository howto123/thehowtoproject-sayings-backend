import { describe, test, expect } from '@jest/globals';

/*
To be written...

describe('Does the startup work?', () => {
	test('Site should go online after startup', () => {
		expect(validate('create', arr[0])).toEqual(emptyObj);
	});
});
*/
